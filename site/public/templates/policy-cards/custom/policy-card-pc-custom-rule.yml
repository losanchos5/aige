# .github/workflows/policy-card-pc-custom-rule.yml: the CI hook of Policy Card pc-custom-rule
# version 1.0.0, rule custom.rule.v1 (effect: deny; enforced at: pre_merge, deploy).
# Generated by the Policy Card builder, https://aigovernanceengineer.com/toolkit/policy-card
# Illustrative, review before use. Indicative, not legal advice and not a conformity claim.
# Pin the action and OPA versions you have tested; the majors below were current on 2026-09-24.
name: policy-card-pc-custom-rule

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  policy-card:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: open-policy-agent/setup-opa@v2
        with:
          version: latest
      - name: Check and test the rule
        run: |
          opa check --strict policies/pc-custom-rule.rego policies/pc-custom-rule_test.rego
          opa test -v policies/pc-custom-rule.rego policies/pc-custom-rule_test.rego
      - name: Build the input
        # Yours: write the input document the module header describes, from the registry,
        # the eval store or the change itself. A compliant example:
        # policies/pc-custom-rule.input.json
        run: ./scripts/policy-input.sh pc-custom-rule > policy-input.json
      - name: Record the verdict
        run: |
          opa eval --format raw \
            --data policies/pc-custom-rule.rego --input policy-input.json \
            'data.aige.cards.pc_custom_rule.verdict' | tee verdict-pc-custom-rule.json
      - name: Keep the verdict as evidence
        uses: actions/upload-artifact@v7
        with:
          name: verdict-pc-custom-rule
          path: verdict-pc-custom-rule.json
      - name: Fail the job on deny
        run: |
          opa eval --format pretty \
            --data policies/pc-custom-rule.rego --input policy-input.json \
            'data.aige.cards.pc_custom_rule.deny'
          opa eval --fail-defined --format pretty \
            --data policies/pc-custom-rule.rego --input policy-input.json \
            'data.aige.cards.pc_custom_rule.blocks'
